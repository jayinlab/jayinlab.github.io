{
  "questions": [
    {
      "difficulty": "beginner",
      "q": "PM4 Type-3 패킷의 역할은?",
      "options": [
        "GPU VRAM에 데이터를 직접 복사",
        "opcode를 통해 GPU command processor에 작업을 지시하는 명령 패킷",
        "CPU-GPU 동기화를 위한 fence 신호",
        "shader 바이너리를 GPU에 업로드"
      ],
      "answer": 1,
      "explanation": "Type-3 패킷은 PM4 명령 체계의 핵심입니다. 헤더에 opcode(IT_DISPATCH_DIRECT, IT_SET_SH_REG 등)가 있고, GPU Command Processor(CP)가 이를 읽어 실제 동작을 수행합니다."
    },
    {
      "difficulty": "beginner",
      "q": "Indirect Buffer(IB)란?",
      "options": [
        "GPU가 compute 결과를 쓰는 출력 버퍼",
        "PM4 패킷들이 순서대로 담긴 메모리 버퍼 — GPU CP가 읽어 실행",
        "CPU가 GPU 메모리를 간접 접근하는 매핑 테이블",
        "command queue의 다른 이름"
      ],
      "answer": 1,
      "explanation": "IB(Indirect Buffer)는 PM4 패킷들의 연속된 시퀀스입니다. UMD가 IB를 채우고 KMD에 제출 요청을 보내면, GPU CP가 ring buffer를 통해 IB를 받아 패킷을 순서대로 처리합니다."
    },
    {
      "difficulty": "intermediate",
      "q": "PM4 패킷을 실제로 조립(생성)하는 레이어는?",
      "options": [
        "KMD — amdgpu.ko 커널 드라이버",
        "GPU hardware — CP가 자체 생성",
        "UMD — RADV, AMDVLK 등 user-space driver",
        "OpenCL runtime — clEnqueueNDRangeKernel 내부"
      ],
      "answer": 2,
      "explanation": "PM4 패킷 조립은 user-space의 UMD(RADV/AMDVLK)에서 합니다. vkCmd* 호출이 UMD 내부에서 PM4 패킷으로 변환되어 IB에 기록됩니다. KMD(amdgpu.ko)는 ring buffer 관리와 DRM_AMDGPU_CS ioctl로 IB 제출만 처리합니다."
    },
    {
      "difficulty": "intermediate",
      "q": "DRM_AMDGPU_CS ioctl의 역할은?",
      "options": [
        "PM4 패킷을 생성하여 GPU에 직접 전송",
        "UMD가 준비한 IB를 KMD에 전달 — KMD는 ring buffer에 등록하여 CP가 실행하도록 함",
        "GPU 메모리를 할당",
        "shader를 컴파일하여 GPU에 로드"
      ],
      "answer": 1,
      "explanation": "UMD는 IB를 채운 뒤 DRM_AMDGPU_CS ioctl로 KMD에 '이 IB를 실행해줘'를 요청합니다. KMD는 해당 IB 주소를 ring buffer에 등록하고, GPU CP가 ring buffer를 폴링하다가 IB를 실행합니다."
    },
    {
      "difficulty": "intermediate",
      "q": "ring buffer는 어느 레이어가 관리하는가?",
      "options": [
        "UMD (RADV/AMDVLK)",
        "OpenCL runtime",
        "KMD (amdgpu.ko)",
        "GPU CP가 직접 관리"
      ],
      "answer": 2,
      "explanation": "ring buffer는 KMD(amdgpu.ko)가 관리합니다. UMD는 IB를 만들어 KMD에 제출 요청만 하고, KMD가 write pointer를 갱신하며 ring buffer에 IB 주소를 기록합니다. GPU CP는 read pointer를 진행시키며 ring buffer를 소비합니다."
    },
    {
      "difficulty": "advanced",
      "q": "IT_DISPATCH_DIRECT 패킷을 받은 GPU CP가 수행하는 동작은?",
      "options": [
        "즉시 모든 CU에서 shader 코드를 실행",
        "패킷의 dimX/Y/Z를 파싱하여 work-group들을 compute unit에 스케줄링",
        "CPU에 인터럽트를 발생시켜 실행 허가를 요청",
        "VRAM에서 shader 바이너리를 로드"
      ],
      "answer": 1,
      "explanation": "CP는 IT_DISPATCH_DIRECT의 dim 값을 파싱하고 wavefront들을 CU(Compute Unit)에 배분합니다. shader 바이너리 주소와 SRD(Shader Resource Descriptor) 정보는 사전에 SET_SH_REG 패킷으로 레지스터에 세팅되어 있습니다."
    },
    {
      "difficulty": "advanced",
      "q": "여러 submit(IB)이 ring buffer에 쌓일 때, GPU CP와 CPU의 동기화 방법은?",
      "options": [
        "CPU가 ring buffer를 직접 폴링",
        "GPU CP가 IB 실행 완료 후 fence value를 메모리에 write → CPU가 fence value를 확인",
        "KMD가 매 패킷 실행마다 인터럽트를 발생",
        "ring buffer가 비워지면 자동으로 CPU에 알림"
      ],
      "answer": 1,
      "explanation": "fence 메커니즘을 사용합니다. UMD가 IB 끝에 WRITE_DATA 패킷(fence value 기록)을 삽입하고, CPU는 해당 메모리 주소의 값이 예상 값이 될 때까지 폴링(또는 wait ioctl)합니다. clFinish/vkQueueWaitIdle이 이 경로를 사용합니다."
    }
  ]
}